# 复制为 .env 并填入 key（server 启动时自动读取），或在命令行 export。
# 没有任何 key 也能跑：自动进入「示例模式」，返回内置样例课。

# —— 文本生成（二选一即可；都配则优先用 Claude）——
# Anthropic Claude（生成正文/大纲/追问）
ANTHROPIC_API_KEY=sk-ant-xxxxxxxx
# 阿里云 DashScope（百炼）——也可单独用它生成正文（Qwen），并提供配图与联网检索
DASHSCOPE_API_KEY=sk-xxxxxxxx

# —— 可选模型 ——
# ANTHROPIC_MODEL=claude-sonnet-4-6              # Claude 生成正文（质量优先）
# ANTHROPIC_ASK_MODEL=claude-haiku-4-5-20251001 # Claude 追问/大纲（速度优先）
# DASHSCOPE_MODEL=qwen-plus                      # Qwen 生成正文
# DASHSCOPE_SEARCH_MODEL=qwen-plus              # 联网检索所用 Qwen
# DASHSCOPE_IMAGE_MODEL=wanx2.1-t2i-turbo       # 通义万相配图模型
# DASHSCOPE_IMAGE_SIZE=1280*720                 # 配图尺寸

# —— 其它 ——
# PORT=8000
# MOCK=1     # 强制示例模式（不调用任何 API）
