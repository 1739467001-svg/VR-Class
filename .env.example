# 复制为 .env 并填入你的 Key（server 启动时会自动读取），或直接在命令行 export。
# 没有 Key 也能跑：会自动进入「示例模式」，返回内置样例课。

# Anthropic API Key（接真 AI 必填）
ANTHROPIC_API_KEY=sk-ant-xxxxxxxx

# 可选：生成课程所用模型（质量优先，默认 claude-sonnet-4-6；要更强可设为 Opus 档）
# ANTHROPIC_MODEL=claude-sonnet-4-6

# 可选：课堂追问所用模型（速度优先，默认 claude-haiku-4-5-20251001）
# ANTHROPIC_ASK_MODEL=claude-haiku-4-5-20251001

# 可选：端口（默认 8000）
# PORT=8000

# 可选：强制示例模式（不调用 API，返回内置样例课）
# MOCK=1
